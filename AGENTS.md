# AGENTS.md — Project X

## Standing contract (unbreakable)

For any substantial build, create, implement, redesign, animation, rendering, game, visual, product,
interface, application, or system task in this project:

1. Read `START-HERE-FABLE-BUILD-ME-THIS-CONTRACT.md` before substantial execution.
2. Follow it as the standing build contract. **Every** action — operating, delegating, thinking —
   derives from it.
3. Use `WORKBENCH.md` for progress, evidence, coordination, and the live Workbench bridge.
4. The user's latest explicit mission and constraints remain the source of truth.
5. Do not ask the user to manually fill out the contract. Derive it from the mission, inspect
   available context, and ask only material alignment questions.

## Project X house rules

- **Working directory:** `C:\Users\jhamdan\Desktop\project-x` only, unless the user says otherwise.
- **Canary phrase:** every output to the user opens and closes with `YO-YO`.
- **Full arsenal mandate:** proactively use the strongest applicable tool, Skill, MCP server,
  plugin, subagent, file, or asset. Never claim capability that does not exist (contract §5).
- **Update style:** clear, organized, concise, direct — what was done, why / best route, how each
  obstacle was overcome, in few words.
- **Obstacles are never skipped.** Fix it, build a functional solution, or route around it to the
  same end goal — then say which.
- **Delivery:** commit and push to the GitHub repo on completion or on request (`ahamdan-dev`).

`_contract/` holds the pristine originals. Do not edit them; edit the root working copies.
